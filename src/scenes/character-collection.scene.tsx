import * as React from 'react';
import { AppLayout } from 'layouts';
import { CharacterContainer } from 'pods/character-collection';

export const CharacterCollectionScene = () => (
  <AppLayout>
    <CharacterContainer />
  </AppLayout>
);
